# Figures Directory

This directory is reserved for final selected manuscript figures.

Most generated figures are written to `outputs/` and ignored by git because high-resolution PNG/TIFF/PDF files can be large. Copy only the final selected figures here when preparing a submission package or public release.

